# The project explaination

# to create 'requirements' file


# How to ru
0) Download requirments from the 'requirements' file (use 'pip install -r requirements')
1) Clone git to your compter
2) From the folder "HEBNLP", run the command "gui/gui.py"
3) Choose model from the list
4) Write your text into the text box
5) Press 'enter' to predict emoji


things to do:
1. requirments file//+readme
2. more models 
3. finish the report
4. the \r event, is it the same in exh os? check
5. todo in the code to check
6. problem with the chosen model...stuck on the previous
